#ifndef LIBFT_H
# define LIBFT_H

# include <stdlib.h>
# include <unistd.h>
# include <stddef.h>

int		ft_digit(int c);
size_t	ft_strlen(const char *s);
int		ft_toupper(int x);
char	*ft_strdup(const char *s1);

#endif
